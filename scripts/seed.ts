/**
 * Script de seed migrado para o backend NestJS + Prisma.
 *
 * Para executar o seed:
 *   npm run backend:seed
 *
 * Ou manualmente:
 *   cd backend
 *   npm run prisma:seed
 *
 * O script de seed está em: backend/prisma/seed.ts
 */

console.log('Este script foi migrado para backend/prisma/seed.ts');
console.log('Execute: npm run backend:seed');
